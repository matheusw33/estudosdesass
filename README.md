# estudosdesass
Nesse curso de SASS aprendi como fazer iterações no Sass com for, each ou while usando os At Rules. Criar condicionais no Sass com @if, @else if e @else e por fim como usar o While no Sass.
